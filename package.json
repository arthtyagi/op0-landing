{
  "name": "op0-landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy:alchemy": "ALCHEMY_STAGE=preview alchemy deploy",
    "deploy:alchemy:prod": "ALCHEMY_STAGE=prod alchemy deploy",
    "deploy:wrangler": "node scripts/build.js && wrangler deploy --config wrangler.jsonc"
  },
  "devDependencies": {
    "alchemy": "next",
    "wrangler": "^4"
  }
}
